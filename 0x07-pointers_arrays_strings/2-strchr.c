#include "main.h"
#include <string.h>

/**
 * _strchr - a function that locates a character in a string
 * @s: string
 * @c: a character
 * Return: a poiter to c if found or NULL if not found
 */
char *_strchr(char *s, char c)
{
	char _strchr[]= "hello./0"
		
