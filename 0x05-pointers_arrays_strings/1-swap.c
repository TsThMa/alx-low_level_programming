#include "main.h"

/**
 * swap_int - a function that swaps the values of two integers.
 * @a: an integer, that will swap values with another integer.
 * @b: an integer, that will swap values with another integer.
 * Return: nothing
 */
void swap_int(int *a, int *b)
{
	*a = 42;
	*b = 98;
}
