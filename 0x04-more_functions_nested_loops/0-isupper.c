#include "main.h"
#include <stdio.h"

/**
 *main - check the code.
 *
 *return 1 if uppercase, return 0 otherwise
 */
int main(void)
{
	    
	    char c;
	        c = 'A';
		    
		    if (c >= 65 && c <= 90)

			        return (1);
		        else return (0);
}

