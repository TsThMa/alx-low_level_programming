#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 *
 *return 1 if uppercase, return 0 otherwise
 */
int main(void)
{
char c;
int _isupper(int c);

c = 'C';

	if (c > 66 && c < 91);
{
	return (1);
		_isupper(c);
		_isupper('\n');
}

	else (c < 64 && c > 91);
{
		return (0);
}
		_isupper('\n');
}

